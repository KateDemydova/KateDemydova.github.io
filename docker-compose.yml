services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      NOD_ENV: development
      MONGO_URI: mongodb://myuser:mypassword@mongo_main:27017/users?authSource=admin
      DB_NAME: users
    volumes:
      - .:/app
      - /app/node_modules
    command: npx nodemon --legacy-watch -L src/server.mjs
    depends_on:
      - mongo_main

  mongo_main:
    image: mongo:latest
    command: [ "mongod", "--quiet", "--logpath", "/dev/null" ]
    ports:
      - '27017:27017'
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example
    volumes:
      - ./db:/data/db